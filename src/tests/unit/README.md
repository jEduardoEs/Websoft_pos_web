# src/tests/unit/

## Responsabilidad
Pruebas unitarias de funciones de lógica de negocio (`logic/`), validadores (`validators/`) y mappers (`mappers/`) sin dependencias de base de datos ni interfaz de usuario.
