# tests/ (Pruebas Automatizadas del Módulo)

## Responsabilidad
Estructura para pruebas unitarias (`unit/`) de los casos de uso (`logic/`) y mappers (`mappers/`), así como pruebas de integración de repositorios (`integration/`).
